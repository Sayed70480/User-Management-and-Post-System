<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Data Association Mini Project</title>
    <link rel="stylesheet" href="/Style/style.css" />
    <link
      href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="min-h-full bg-zinc-900 text-white p-6">
      <div class="flex justify-end gap-4">
        <a
          href="/profile/upload"
          class="flex mt-6 w-52 mb-10 justify-center rounded-md bg-green-600 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600"
          >Upload Profile Picture</a
        >
        <a
          href="/logout"
          class="flex mt-6 w-28 mb-10 justify-center rounded-md bg-red-600 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600"
          >Logout</a
        >
      </div>
      <div class="flex gap-4 justify-center items-center">
        <div class="w-12 h-12   "><img class="w-full h-full object-cover rounded-3xl" src="/Images/upload/<%= user.profilepic %>" alt=""></div>
        <h2 class="font-medium text-2xl">
          Hello! <%= user.name %> Welcome to Profile
        </h2>
       
      </div>
      <h2 class="font-medium text-xl text-center">You can Create a New Post</h2>
      <form action="/post" method="POST">
        <textarea
          name="content"
          placeholder="What's on your mind?"
          class="w-full mt-4 bg-transparent border-2 border-indigo-600 outline-none rounded-md p-2"
        >
        </textarea>
        <button
          type="submit"
          class="flex mt-6 w-full justify-center rounded-md bg-indigo-600 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600"
        >
          Submit
        </button>
      </form>

      <div class="posts mt-10">
        <h3 class="text-xl font-medium">Your Post's</h3>
        <div class="postContainer w-full flex gap-4 flex-wrap ">
          
       <%
       user.posts.reverse().forEach((post)=>{
        %>

        <div
        class="post w-1/3 border-[1px] border-indigo-600 mt-8 p-4 rounded-md bg-zinc-800"
      >
        <h4 class="text-indigo-600 font-medium text-xl mb-4">@<%= user.username %></h4>
        <p class="mb-4 text-justify flex flex-wrap overflow-auto">
          <%= post.content %>
        </p>
        <div class="flex justify-between">
          <div class="flex gap-4">
            <a href="/like/<%= post._id %>">
              <% if (post.likes.indexOf(user._id) === -1) { %>
                <i class="unlike bx bxs-heart text-3xl"></i>
              <% } else { %>
                <i class="bx bxs-heart text-3xl"></i>
              <% } %>
            </a>
            <h4 class="mt-1"><%= post.likes.length %></h4>
          </div>
          <button
          class="flex  justify-center rounded-md bg-indigos-600 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600"
        >
          <a href="/delete/<%= post._id %>">Delete</a>
        </button>
          <button
            class="flex  justify-center rounded-md bg-indigos-600 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600"
          >
            <a href="/edit/<%= post._id %>">Edit</a>
          </button>
        </div>
      </div>

       <%})
       
       %>
      
       
        </div>
      </div>
    </div>
  </body>
</html>
